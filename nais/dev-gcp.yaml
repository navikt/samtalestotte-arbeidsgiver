apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: samtalestotte-arbeidsgiver
  namespace: arbeidsgiver
  labels:
    team: arbeidsgiver
spec:
  image: {{image}}
  team: arbeidsgiver
  port: 3000
  ingresses:
    - https://arbeidsgiver-gcp.dev.nav.no/samtalestotte-arbeidsgiver
  liveness:
    path: /samtalestotte-arbeidsgiver/internal/isReady
    initialDelay: 10
  readiness:
    path: /samtalestotte-arbeidsgiver/internal/isReady
    initialDelay: 10
  accessPolicy:
    outbound:
      external:
        - host: dekoratoren.dev.nav.no
  envFrom:
    - secret: forebygge-sykefravaer-secrets
